## Task2 in 6th Homework
### How to run
1. Install gunicorn
    ```sh
   pip install gunicorn
    ```
2. cd into this folder (where README is placed)
    ```sh
   cd <path>
    ```
3. Just run gunicorn
    ```sh
    gunicorn
    ```
